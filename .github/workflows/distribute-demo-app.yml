name: Distribute Warp Demo App

on:
  # Manual trigger from GitHub Actions UI or `gh workflow run`.
  # Also dispatched automatically by the `release-warp` skill after a successful Artifactory publish.
  workflow_dispatch:
    inputs:
      release_note:
        description: 'Release notes'
        required: false
        default: 'Warp demo app build'
        type: string

jobs:
  distribute:
    name: Build and distribute demo app
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v3

      - name: Build debug APK
        run: ./gradlew app:assembleDebug

      - name: Upload to Firebase App Distribution
        uses: wzieba/Firebase-Distribution-Github-Action@v1
        with:
          appId: ${{ secrets.FIREBASE_APP_ID }}
          serviceCredentialsFileContent: ${{ secrets.FIREBASE_CREDENTIALS }}
          groups: warp-android-testers
          releaseNotes: ${{ inputs.release_note || 'Warp demo app - automated build' }}
          file: app/build/outputs/apk/debug/app-debug.apk
